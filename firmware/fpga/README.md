# Audio FPGA (EA3 / EA5)

Placeholder. The multi-zone EA3/EA5 carry an FPGA in the audio path (seen in the
stock kernel's `sound/soc/ninjago/ninjago-fpga-*` drivers). Bitstreams / load
sequences for it would live here. The EA1 has no such FPGA — audio is straight
off the SoC — so nothing is needed for the current target.
